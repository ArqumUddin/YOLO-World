input:
  path: "/home/ubuntu/Workspace/data/sdg"
  type: "directory"

output:
  directory: "inference_results/"
  save_annotated: true
  save_json: true
  results_filename: "results.json"

visualization:
  bbox_thickness: 2
  font_scale: 0.5
  show_confidence: true
  show_class_name: true

client:
  server_url: http://localhost:12182/yolo_world
